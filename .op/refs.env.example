# Copy to .op/refs.env and set the cadena-sh Environment UUID from
# 1Password → Developer → Environments → cadena-sh → Manage environment.
# .op/refs.env is gitignored; do not commit a real Environment UUID.
CADENA_SH_DEV_1PASSWORD_ENVIRONMENT_ID=
