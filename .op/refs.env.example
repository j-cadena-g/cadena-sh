# Copy to .op/refs.env and paste the UUID of a 1Password Environment you own.
# Create one under 1Password → Developer → Environments, add the contact-form
# variables from the README, then copy the UUID from Manage environment.
# .op/refs.env is gitignored; do not commit a real Environment UUID.
CADENA_SH_DEV_1PASSWORD_ENVIRONMENT_ID=
